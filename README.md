# docker-sidekiqweb

This differs only from https://github.com/maddiefletcher/docker-sidekiqweb in that it uses `REDIS_NAMESPACE` (if it is set as an environment variable) as the `namespace` configuration parameter.

## Maintenance

Other maintenance commits have been made to keep components updated. It might be worth rebasing this from the original repo, which has had some updates since I forked this.